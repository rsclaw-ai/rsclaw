//! Auto-approve handler for `cap_rs::AgentEvent::PermissionRequest`.
//!
//! P1 mirrors the legacy `src/acp/client.rs` policy: every request is
//! responded to with `PermissionDecision::Allow`. Real human-in-the-
//! loop is P2 (conversation mode).

// Module body to be filled in Task 4.
