//! `cap_rs::AgentEvent` → rsclaw sinks dispatch.
//!
//! Pure mapping layer. The only file in `src/cap/` that knows about
//! rsclaw types (`NotificationSink`, `events::AgentEvent`). Keeps
//! `runtime.rs` and `permission.rs` independent of rsclaw internals.

// Module body to be filled in Task 3.
